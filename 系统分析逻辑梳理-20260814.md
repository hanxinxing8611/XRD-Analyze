# XRD 图谱分析系统逻辑梳理（参考文档）

**日期**：2026-08-14　|　**对象**：`XRD图谱分析App`（纯前端单页应用：HTML + D3.js v7，本地浏览器运行，无后端）
**数据流总览**：导入数据 → 解析 → 预处理（仅用于质量统计/基线显示）→ 峰检测 → 主相匹配/掩膜 → 杂质峰检测 → 杂质相识别 → 综合指标（FWHM/晶粒/结晶度/晶格/RIR/不确定度）→ 结论与导出（PNG/PDF/CSV）

---

## 1. 谱图绘制方法与修正逻辑

### 1.1 绘制（drawChart）
- **画布**：D3 SVG；X 轴 2θ(°)，Y 轴强度(counts)；线性比例尺；网格 + 坐标轴（瀑布图模式隐藏 Y 刻度）。
- **配色**：标准样=蓝色实线 `#3b82f6`，测试样=荧光绿实线 `#34d399`；瀑布图模式按导入顺序着色并纵向偏移（测试样在上、标准样最底）。
- **叠加元素**：
  - 理论峰位：蓝色虚线竖线（来自 standardData）；
  - 杂质峰标记：琥珀色圆点 `#fbbf24` + 文字“杂质N”；
  - 基线（可选叠加显示）、高亮峰、隐藏样品。
- **交互**：缩放/平移（d3 zoom + clipPath）、对数/线性 Y 轴切换、瀑布图开关、基线开关、点击杂质峰行高亮对应位置。
- **关键约定**：图表与**分析均使用原始测量数据**（rawData），不使用预处理结果，避免平滑/扣背景/Kα₂ 剥离在峰位附近产生削峰、凹陷伪影。预处理结果仅用于 SNR 统计与基线叠加显示。

### 1.2 数据预处理（preprocessData）——仅统计/显示用
1. **Savitzky-Golay 平滑**：窗口 9、3 阶。
2. **AsLS 基线校正**：λ=1e6、p=0.001、迭代 8；基线截断为非负（防边缘/强峰过冲）。
3. **扣除背景**：平滑 − 基线，非负。
4. **Kα₂ 剥离**（Rachinger 法）：仅用于 SNR 计算参考；出现 NaN 时回退用扣背景数据。
5. **SNR**：信号峰值 / 最低 25% 强度均方根噪声；质量分级：优秀>50、良好>20、一般>10、较差。

### 1.3 峰位系统偏移修正
- `findSystematicShift`：用显著峰（净高 ≥ 2% 主峰）在 ±0.30° 内扫描偏移，取**匹配峰数最多 → 平均偏差最小**的偏移为系统偏移 shift。
- 主相掩膜、杂质峰屏蔽、杂质匹配均按该 shift 平移（样品高度/装样误差等整体偏移被吸收）。

---

## 2. 标准峰位、谱图数据来源

### 2.1 理论峰位线（蓝色虚线，standardPeakData / loadStandardData）
- 8 个产品：**CsPbBr₃ / CsPbI₃(δ-黄相) / FAPbBr₃ / δ-FAPbI₃ / α-FAPbI₃ / MAPbBr₃ / MAPbI₃ / C₆₀**。
- 数据源：**JCPDS PDF 卡片 + 公开文献（COD/ICSD）晶胞计算值**，Cu Kα λ=1.5406 Å，强度为相对强度 0–100。
- 各产品晶胞与参考：
  - CsPbBr₃：正交 Pnma（a=8.207, b=8.255, c=11.759 Å），JCPDS 00-054-0752 + 单晶 XRD 文献，7 条峰；
  - CsPbI₃：**δ-黄相** 正交 Pnma（COD 4127358 / ICSD 27979），27 条峰（最强 37.73°）；
  - FAPbBr₃：立方 Pm-3m（a=6.0061 Å，Sci. Rep. 2016, 6, 36733），8 条峰；
  - δ-FAPbI₃：六方 P63mc（a=8.6603, c=7.9022 Å），19/22 条峰；
  - α-FAPbI₃：立方 Pm-3m（a=6.362 Å），13 条峰（2026-08-06 补全高阶线）；
  - MAPbBr₃：立方 Pm-3m（COD 1545320），8 条峰；
  - MAPbI₃：四方 I4/mcm（共识晶胞 a=8.849, c=12.642 Å），19 条峰（2026-08-06 统一）；
  - C₆₀：JCPDS 44-0558，23 条峰。

### 2.2 标准样品实测谱图（蓝色实线）
- **实测标准样**（`标准样/*.uxd`，Bruker 实测，5–55°，2572 点）：CsPbBr3、CsPbI3(δ)、FAPbBr3、MAPbBr3、δ-FAPbI3；
- **合成标准样**（权威晶胞计算生成，`参考数据/gen_uxd/`）：α-FAPbI3（2501 点）、MAPbI3（2501 点）、C60（2751 点）；
- **内嵌备份**：`EMBEDDED_STANDARD_DATA` 将完整标准谱图内嵌在 index.html，标准文件缺失时自动使用；
- 选择产品 → 自动加载对应标准样（可手动上传替换）。

### 2.3 主相掩膜 MAIN_PHASE_FULL
- 每个产品一组**完整峰位**（含较弱/高阶线），用于主相拟合质量判断与杂质峰屏蔽；如 δ-CsPbI₃ 27 条、MAPbI₃ 42 条（COD 计算）、α-FAPbI₃ 18 条等。

### 2.4 杂质数据库 GLOBAL_DATABASE（22 个相）
- 碘系(I)：PbI₂、CsI、FAI、MAI、δ-CsPbI₃、δ-FAPbI₃、MA₂PbI₄、FA₂PbI₄、Cs₄PbI₆、PbI₂·DMSO、MAPbI₃·hydrate；
- 溴系(Br)：PbBr₂、CsBr、Cs₄PbBr₆、CsPb₂Br₅、**δ-FAPbBr₃（2026-08-12 新增，按实测标定 12.37/37.40/39.33°）**；
- 双系(both)：Pb、PbO-α、Pb₃O₄、PbO₂-α、PbO₂-β、graphite。
- 每条目：name / formula / category（前驱体残留/富铯副产物/富铅副产物/氧化产物/金属单质/有机盐残留/非钙钛矿相/层状相等）/ groups / peaks(2θ, 强度, hkl) / source / url / note；来源为 COD/ICSD/JCPDS 计算 + 文献 + 实测样品标定。

### 2.5 标准数据核查与修正记录
- 2026-08-05 标准样修正：α-FAPbI3.uxd 实为 FAPbBr3 谱图平移 -0.80°（已替换为权威合成谱图）；MAPbI3 晶胞偏小（已替换）；CsPbI3 采用 δ-黄相为标准（黑相α归档）。
- 2026-08-06 全量核查：MAPbI₃ 统一共识晶胞并校正 (211)/(202) hkl；α-FAPbI₃ 补全 13 条高阶线；其余 6 产品理论/实测偏差 ≤0.15° 正常。
- 备份：index.html.bak-20260805-beforeStdCorrection、.bak-20260807-beforeP0、.bak-20260808-beforeP1。

---

## 3. 杂峰、杂项分析及判断逻辑

### 3.1 峰检测（findAllPeaksInData）
- 对数据做**局部极大检测**（±4 点窗口），估计峰高/净高/FWHM；0.08° 内近峰合并。
- 备选二阶导数法（findPeaks：5 点二阶导数 + 凹区 + 局部极大 + 峰位精修 + 0.3° 合并）。

### 3.2 主相拟合质量
- 强掩膜峰（理论强度 ≥15 且在范围内）≥3 个，但实际命中（净高 ≥2% 主峰、±0.30° 内）<3 → **主相拟合差**（fitPoor），提示“可能选错产品/样品已相变/掺杂”，按未知主相处理。

### 3.3 主相屏蔽
- 峰位在掩膜峰 ±0.35°（含系统偏移）内 → 归主相；
- 主相强峰旁 0.25° 内、净高 < 强峰 50% → 作主相肩峰屏蔽。

### 3.4 杂质峰候选检测（2026-08-13 优化，P0-4/P0-5）
判定一条“未被主相解释的峰”是否为杂质峰：
1. **稳健净高 rn** = 峰高 − ±0.7° 窗口内 10% 分位背景；
2. **锐度**：局部净高（±4 点）≥ 0.40×rn；
3. **两侧肩部显著性（P0-5）**：promNet = 峰高 − 两侧（±0.70~±0.20°）较高背景（15% 分位）≥ 0.35×rn —— 滤除 20° 内低角背景隆起/斜坡/测量起始区假峰；
4. 门槛：rn ≥ 3×噪声（MAD 估计）、rn ≥ 8% 峰高、rn ≥ 1.2% 主峰净高、FWHM ≤ 3.0°；
5. **保留规则**：rn ≥ 3% 主峰，或与杂质库任一峰匹配（±0.35°）；
6. 按强度降序，最多输出 25 个。

### 3.5 杂质相识别（identifyImpurities，Hanawalt 式综合评分）
- **家族过滤**：仅考虑与主相同族（I/Br）或 both 相；排除主相自身；
- 每相独立做**小范围偏移搜索**（±0.12~0.30° 内、步长 0.05°），**贪心一对一匹配**（每个杂质峰最多用一次）；
- **评分**（满分约 100）：
  `score = strongRefRatio×40 + top3Count/3×25 + positionScore×15 + coverage×20`
  - strongRefRatio：参考相中强度≥30 的峰被匹配比例；
  - top3Count：参考相最强 3 峰中被匹配数量；
  - positionScore：位置偏差得分均值（1−Δ/0.35）；
  - coverage：被该相解释的杂质峰比例；
- **修正系数**：top3 未全中 ×0.75；strongRefRatio<0.5 ×0.6；存在未解释强峰 ×(1−0.2×n)；仅匹配 1 峰上限 20；最强匹配峰 <2% 主峰上限 20；主相拟合差且覆盖率<0.5 上限 35；
- **置信度**：确认 / 可能 / 暂定 / 存疑（按 top3、strongRefRatio、匹配数≥3、覆盖率组合）；
- **P0-4 综合去重**：同一杂质峰优先归属得分最高的相；仅靠“已被更优相解释峰”支撑的相降级为存疑，避免误报/重复（如 δ-FAPbI₃ 峰被误配 CsI/PbI₂）；
- 按 score 降序输出。

### 3.6 单峰匹配（matchWithDatabase，明细表用）
- 容差 ±0.4°；`score = 100 − Δ/0.4×50`（下限 50）；每峰返回最佳 2 个候选。

### 3.7 展示逻辑（analyzeImpurities）
- **有效杂质相**：score ≥ 25 的相显示在“可疑杂质相”汇总表（最多 6 个）；
- **杂质峰明细**：被有效相解释 或 强度 ≥ 5% 主峰的峰显示；其余为“未归属弱峰”；
- 图上琥珀色标记“杂质N”；点击峰行在图谱高亮。

---

## 4. 分析结果计算逻辑

### 4.1 数据质量（renderDataQuality）
- SNR、噪声 RMS、信号峰值、点数、2θ 范围、预处理方法说明；质量分级（优秀/良好/一般/较差）。

### 4.2 FWHM（calculateFWHM）
- 以理论峰位为中心，峰高一半处左右**线性插值**求半高宽（° 与 rad）。

### 4.3 峰面积（integratePeakArea）
- 理论峰位 **±1.0°** 区间**梯形积分**。

### 4.4 晶粒尺寸（calculateCrystalliteSize，Scherrer）
- `D = K·λ / (β·cosθ)`，K=0.89，λ=0.15406 nm，β=FWHM(rad)；**简化版未扣仪器展宽**（结果偏小/仅供参考）；输出 nm 与 Å。

### 4.5 结晶度（calculateCrystallinity）
- **简化分峰法**：所有结晶峰（±1.0° 积分面积）之和 / 全谱总面积 ×100，上限 100%。

### 4.6 晶格参数（refineLatticeParams）
- 立方晶系：由每个实测峰位 → 布拉格 d → `a = d·√(h²+k²+l²)`（hkl 取理论标注），多峰求均值/标准差；输出 nm 与 Å。

### 4.7 RIR 半定量（calculateRIRComposition）
- **简化 RIR**：主相归一化为 1.0；杂质相取各自**最强匹配峰强度**；`含量 = raw/(1.0+Σraw)×100`；
- 仅“确认/可能”置信度相参与；结果为**相对强度占比，非质量分数**（界面/导出均有注明）。

### 4.8 测量不确定度（calculateUncertainty）
- 各理论峰与样品最近峰的 |Δ2θ|：均值、标准差、最大/最小偏差、95% 扩展不确定度（σ×1.96）、样本数。

### 4.9 峰位对比表（updatePeakComparisonTable）
- 每条理论峰：样品最近峰位/强度、偏差、状态（匹配 <0.2° / 偏移 <0.5° / 缺失）；可导出 CSV。

### 4.10 综合分析流程（runComprehensiveAnalysis）
- 顺序：数据质量 → FWHM/晶粒/峰面积 → 结晶度 → 晶格参数 → **杂质分析**（analyzeImpurities）→ RIR 半定量 → 不确定度；结果写入 analysisResults 缓存供导出使用。

### 4.11 结论文本（getAnalysisConclusionText）
- 无杂质峰 → 主相吻合、纯度良好；
- 有杂质峰 → 按“确认/可能/暂定/存疑”分级列出；无已知相匹配则建议结合 SEM/TEM/EDS 等确认；并提示主相拟合警告与“相对强度非质量分数”。

### 4.12 导出
- PNG（html2canvas）、PDF（jsPDF：图表 + 峰位对比 + 杂质明细 + 结论）、CSV（峰位对比/批量分析）。

---

## 5. 版本与更新记录
- 2026-08-05：标准样修正（α-FAPbI₃/MAPbI₃ 替换为权威合成谱图，CsPbI₃=δ 黄相）。
- 2026-08-06：标准数据全量核查（MAPbI₃ 共识晶胞、α-FAPbI₃ 补全高阶线）。
- 2026-08-12：新增杂质相 δ-FAPbBr₃（37.4/39.3° 识别）。
- 2026-08-13：P0-4 弱峰检测（稳健净高+锐度+综合去重）；P0-5 两侧肩部显著性（滤除 20° 内低角假峰）；标题栏毛玻璃样式迭代。
